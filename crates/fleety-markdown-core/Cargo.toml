[package]
name = "fleety-markdown-core"
description = "Vendored headless markdown analysis (see ../fleety-markdown/README.md for provenance)."
# Vendored third-party source: edition is the upstream crate's own, not the
# workspace value. See ../fleety-markdown/README.md.
edition = "2024"
rust-version = "1.85"
version.workspace = true
license.workspace = true
repository.workspace = true
publish.workspace = true

[dependencies]
pulldown-cmark = "0.13"
