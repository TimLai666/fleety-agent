## 1. Protocol wire — 2a(design「分兩階段落地(2a wire 先、2b 面板後)」;Protocol:結構化 config 通道 + 能力協商 + 未知 frame 容忍(補 M3/M4))

- [x] 1.1 在 crates/fleety-protocol/src/lib.rs 新增 `ClientMsg::ConfigSnapshot { target }`、`ClientMsg::ConfigApply { target, base_revision, changes: Vec<ConfigChange> }`、`ServerMsg::ConfigSnapshotResult { revision, entries: Vec<ConfigEntry>, providers_json }`,型別 `ConfigEntry { key, scope, value, default, description, secret, is_set, effect, choices }`、`ConfigChange { key, op: ChangeOp(Keep|Set|Clear), value: Option<String> }`;`Welcome` 加 additive `config_protocol: u32`;`ClientMsg` 加 `#[serde(other)] Unknown` catch-all;`PROTOCOL_VERSION` +1(design「Protocol:結構化 config 通道 + 能力協商 + 未知 frame 容忍(補 M3/M4)」),交付「The protocol negotiates capability and tolerates unknown frames」+「The server exposes its settings as a structured snapshot」的 wire 型別。單元:新 frame/型別 serde round-trip、additive 舊端可解析、Unknown catch-all 解析未知 type。

## 2. Server wire — 2a

- [x] 2.1 [P] 在 crates/fleety-tools/src/config.rs 新增結構化 snapshot 資料(每 key 的 scope/default/description/secret/is_set/effect/choices,以 fleety-tools 自有 struct 回,server 端映射到 protocol ConfigEntry,保持 fleety-tools 不依賴 protocol)、`revision(path) -> String`(內容 hash),並把 `save` 改 tmp+rename+單一 per-file mutex、壞檔回明確錯誤不 fail-soft(design「Server:config.toml 真原子存檔(補 M3)」),交付「Server config is saved atomically and never fail-softs to defaults」。單元:snapshot 資料含各欄位且 secret 只回 is_set、`revision` 對同內容穩定/改動即變、原子存檔 round-trip、壞檔回錯。
- [x] 2.2 在 crates/fleety-server/src/conn.rs 加 `ConfigSnapshot` handler:組 `ConfigSnapshotResult`(entries 由 2.1 資料映射、secret 只 is_set、providers_json 帶結構化 provider/model、revision = 內容 hash + server boot id),secret 讀取記稽核(design「Server:snapshot builder + apply handler…」),交付「The server exposes its settings as a structured snapshot」+「Secrets are snapshot as is-set and applied write-only」的 snapshot 半。單元:snapshot entries secret 只 is_set、providers_json 可解析、revision 帶 boot id。
- [x] 2.3 在 conn.rs 加 `ConfigApply` handler:比對 `base_revision` 不符回 conflict(樂觀鎖)、sparse changes 逐一套用(keep 略過/set 先 validate 再寫/clear 清除)、Server-scope mutate 要求 `auth.required()`(接 auth-default-on)、敏感 key(provider base_url/key、FLEETY_BACKUP_REPO/_TOKEN、oauth endpoint)套用前告警 + 記稽核(design「Server:snapshot builder + apply handler(revision 樂觀鎖 + secret tri-state + 授權)」),交付「Config changes apply atomically under optimistic locking」+「Secrets are snapshot as is-set and applied write-only」的 apply 半 +「Sensitive server-key changes require auth and are warned and audited」的 server 面。單元:base_revision 過期回 conflict 不覆蓋、set/clear/keep 各行為、auth 關閉時 Server-scope apply 拒、敏感 key 走告警/稽核路徑。
- [x] 2.4 在 conn.rs 的 `read_client`/serve loop 把未知 frame 由「解析失敗斷線」改為「回 `ServerMsg::Error { kind:"unsupported" }` 且連線續存」(配合 1.1 的 `ClientMsg::Unknown`),`Welcome` 帶 `config_protocol` 能力值(design「Protocol:結構化 config 通道 + 能力協商 + 未知 frame 容忍(補 M3/M4)」),交付「The protocol negotiates capability and tolerates unknown frames」的 server 面。單元:收到 Unknown frame 回 unsupported 且不回 None(不斷線)、Welcome 帶 config_protocol。

## 3. CLI 面板 — 2b(CLI:能力偵測 + 面板)

- [ ] 3.1 在 crates/fleety-cli/src/config.rs 加能力偵測:依 `Welcome.config_protocol` 決定走結構化 Snapshot/Apply 或退回舊 `ConfigExec`(前向相容),並實作 Snapshot 拉取 + Apply(帶 base_revision + sparse changes + secret tri-state)的用戶端流程(design「CLI:能力偵測 + 面板(2b)」),交付「The server region edits remote settings via the structured channel」的能力偵測 + wire 用戶端半。單元:config_protocol=0 走 ConfigExec、>0 走 Snapshot/Apply 的分支;Apply 帶 tri-state 的組裝。
- [ ] 3.2 新增 crates/fleety-cli/src/config_panel.rs:ratatui 三區面板(Tab 連線/本機/server),連線區複用 Phase 1 connection module、本機區複用 Phase 1 scoped config、server 區由 3.1 的 Snapshot 填入並經 Apply 遠端套用;裸 `fleety config`(TTY)在 main.rs 路由到此面板、非 TTY 退回文字(design「CLI:能力偵測 + 面板(2b)」),交付「Bare fleety config opens a three-region interactive panel」。單元(headless TestBackend):三區切換、server 區 state 由 snapshot 填入 + 編輯組成 Apply changes round-trip。
- [ ] 3.3 在 config_panel.rs 的 server 區實作 secret tri-state(遮罩 write-only、keep/set/clear)、provider 依 type 顯欄位、生效時機(next connection / restart)在 detail pane 標示,敏感 key 編輯前二次確認(design「CLI:能力偵測 + 面板(2b)」),交付「The server region edits remote settings via the structured channel」的 secret/provider/effect 面 +「Sensitive server-key changes require auth and are warned and audited」的 CLI 確認面。單元:secret 編輯只送新值不回寫遮罩、provider 依 type 欄位、敏感 key 前有確認狀態。

## 4. 文件與驗證

- [ ] 4.1 更新 docs/roadmap.md、docs/STATUS.md、docs/design-cli-config.md(Phase 2 出貨、G2 達成);全 workspace 回歸:`cargo test --workspace` 全綠、`cargo run -p fleety-eval -- run crates/fleety-eval/goldens` 15/15 不改 goldens、`cargo clippy --workspace` 無新違規;手動:新 CLI 對新 server 開三區面板改一個 server 設定生效、對舊 server(config_protocol=0)退回 ConfigExec、未知 frame 不斷線。驗證:測試輸出 + 手動面板/相容檢查。
